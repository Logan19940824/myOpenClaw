# Background Remove MCP Skill 示例

使用 AI 自动去除图片背景的 skill。

## 功能

- ✂️ 自动检测并去除图片背景
- 🤖 基于 AI 模型（U2-Net）
- 📦 输出 PNG 格式（透明背景）
- 🔄 支持批量处理

## 安装依赖

```bash
# 安装 Python 依赖
pip install rembg pillow

# 或者使用 uv (更快)
uv pip install rembg pillow
```

## 使用方法

### 1. 直接命令行使用

```bash
# 基本用法
echo '{"input": "photo.jpg", "output": "photo-no-bg.png"}' | python index.py

# 返回结果
{
  "success": true,
  "output_path": "photo-no-bg.png",
  "error": null
}
```

### 2. 在 OpenClaw 中使用

1. 将 skill 放到 `skills/background-remove/`
2. 在 `SKILL.md` 中定义接口
3. 用户发送："帮我去除 photo.jpg 的背景，保存为 photo-no-bg.png"
4. Agent 自动调用 skill 执行

## 原理

```
┌─────────────────────────────────────────────────────────────────┐
│  输入图片 (JPG/PNG)                                             │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  rembg AI 模型                                                   │
│  - 使用 U2-Net 深度学习模型                                      │
│  - 识别前景和背景                                                │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  输出图片 (PNG 透明背景)                                         │
└─────────────────────────────────────────────────────────────────┘
```

## 效果示例

**输入**: 带背景的人物照片
**输出**: 只有人物的透明 PNG

## 注意事项

1. **首次运行较慢** - 需要下载 AI 模型（约 100MB）
2. **处理大图片** - 可能需要更多内存和时间
3. **复杂背景** - 对于非常复杂的背景，效果可能不完美

## 与 OpenClaw 集成

```python
# OpenClaw 会自动读取 SKILL.md 生成调用代码
# 用户只需用自然语言描述需求

用户: "帮我去除 ~/images/selfie.jpg 的背景"
      ↓
Agent: 解析意图，调用 MCP skill
      ↓
MCP: 执行 remove_background()
      ↓
输出: {"success": true, "output_path": "selfie-no-bg.png"}
```

## 测试

```bash
# 创建测试图片
convert -size 200x200 xc:blue test_image.jpg

# 测试去背景
echo '{"input": "test_image.jpg", "output": "test_output.png"}' | python index.py
```

## 进阶使用

### 设置模型大小

```python
from rembg import remove, U2NET_CUSTOM

# 使用自定义模型
output_image = remove(input_image, model=U2NET_CUSTOM)
```

### 批量处理

```python
import glob

for image_path in glob.glob("images/*.jpg"):
    output_path = f"output/{image_path.split('/')[-1]}.png"
    # 调用 skill 处理...
```
