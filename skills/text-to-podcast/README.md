# Text to Podcast Skill

将文本转换为自然流畅的播客音频，使用 Microsoft Edge TTS 技术。

## ✨ 特点

- 🎵 **音质优秀** - 微软 Azure 语音，自然流畅
- 🔑 **免 API key** - 无需注册，直接使用
- 🌏 **多语言支持** - 中文、英文等 50+ 语言
- 👥 **多种声音** - 男声、女声、童声可选
- 📖 **长文本友好** - 适合播客、有声书

## 📦 安装依赖

```bash
pip install edge-tts
```

## 🚀 使用方法

### 1. 命令行使用

```bash
# 基本用法
echo '{"text": "欢迎收听本期播客", "output": "intro.mp3"}' | python3 index.py

# 指定语音
echo '{"text": "Hello podcast", "voice": "en-US-AriaNeural", "output": "hello.mp3"}' | python3 index.py
```

**返回结果：**
```json
{
  "success": true,
  "output_path": "intro.mp3",
  "file_size": 12345,
  "voice": "zh-CN-XiaoxiaoNeural",
  "error": null
}
```

### 2. 在 OpenClaw 中使用

Agent 会自动读取 `SKILL.md`，你只需用自然语言描述：

```
用户: "将这段文字转成播客音频"
Agent: 解析意图，调用 MCP skill
MCP: 执行 text_to_podcast()
输出: {"success": true, "output_path": "podcast.mp3"}
```

## 🎙️ 可用语音

### 中文语音

| 语音 ID | 名称 | 风格 |
|---------|------|------|
| `zh-CN-XiaoxiaoNeural` | 晓晓 | 女声（推荐） |
| `zh-CN-YunxiNeural` | 云希 | 男声 |
| `zh-CN-YunyangNeural` | 云扬 | 男声（新闻） |
| `zh-CN-XiaoyouNeural` | 悠悠 | 童声 |

### 英文语音

| 语音 ID | 名称 | 风格 |
|---------|------|------|
| `en-US-AriaNeural` | Aria | 女声（推荐） |
| `en-US-JennyNeural` | Jenny | 女声 |
| `en-US-GuyNeural` | Guy | 男声 |
| `en-US-ChristopherNeural` | Christopher | 男声 |

**完整语音列表**：https://speech.microsoft.com/portal

## 📖 示例场景

### 播客开场白

```json
{
  "text": "欢迎收听本期播客节目，我是主持人晓晓。今天我们要探讨的话题是人工智能的发展与未来。",
  "voice": "zh-CN-XiaoxiaoNeural",
  "output": "podcast-intro.mp3"
}
```

### 双人对话

```json
// 主持人
{
  "text": "大家好，欢迎来到本期节目。",
  "voice": "zh-CN-YunxiNeural",
  "output": "host.mp3"
}

// 嘉宾
{
  "text": "谢谢邀请，我很荣幸来到这里。",
  "voice": "zh-CN-XiaoxiaoNeural",
  "output": "guest.mp3"
}
```

### 有声书

```json
{
  "text": "第一章：在很久很久以前，有一个美丽的王国...",
  "voice": "zh-CN-XiaoxiaoNeural",
  "output": "audiobook-chapter1.mp3"
}
```

## ⚙️ 工作原理

```
┌─────────────────────────────────────────────────────────────────┐
│  输入文本 (User Input)                                          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  MCP Skill (index.py)                                           │
│  - 解析参数                                                     │
│  - 调用 edge-tts                                               │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  Microsoft Edge TTS Service                                     │
│  - 文本预处理                                                   │
│  - 神经网络语音合成                                             │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  输出 MP3 文件                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🛠️ 进阶使用

### 在 Python 代码中直接使用

```python
import asyncio
import edge_tts

async def main():
    text = "欢迎收听播客"
    voice = "zh-CN-XiaoxiaoNeural"
    output = "podcast.mp3"
    
    communicate = edge_tts.Communicate(text, voice)
    await communicate.save(output)
    
asyncio.run(main())
```

### 调整语速和音量

```python
# edge-tts 不直接支持语速调整
# 可以使用 ffmpeg 后处理

import subprocess

# 语速 1.5x
subprocess.run([
    "ffmpeg", "-i", "input.mp3", 
    "-filter:a", "atempo=1.5", 
    "output.mp3"
])
```

## ⚠️ 注意事项

1. **网络依赖** - 需要网络连接访问微软 TTS 服务
2. **首次运行** - 无需下载模型，直接使用
3. **文本长度** - 单次调用建议不超过 3000 字
4. **特殊字符** - 支持中文、英文、标点符号

## 📊 性能对比

| 方案 | 音质 | 延迟 | 成本 | 难度 |
|------|------|------|------|------|
| **edge-tts** | ⭐⭐⭐⭐⭐ | 快 | 免费 | 简单 |
| Coqui TTS | ⭐⭐⭐⭐⭐ | 慢 | 免费 | 复杂 |
| OpenAI TTS | ⭐⭐⭐⭐⭐ | 快 | 付费 | 简单 |
| 阿里云 TTS | ⭐⭐⭐⭐ | 快 | 付费 | 中等 |

## 🐛 常见问题

### Q: 生成的音频有噪音？
A: 微软服务稳定，噪音通常是网络问题，重试即可。

### Q: 支持台语、粤语吗？
A: edge-tts 主要支持标准普通话和英文，其他方言支持有限。

### Q: 可以导出其他格式吗？
A: 默认导出 MP3，如需 WAV 可用 ffmpeg 转换。

## 📝 License

基于 MIT License，参考 [edge-tts](https://github.com/rany2/edge-tts)。
