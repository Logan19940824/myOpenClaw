# Video Clipper Skill

自动剪辑视频，支持裁剪、合并、添加字幕、背景音乐，生成可直接发布的短视频。

## 功能特点

- ✂️ **视频裁剪** - 精确裁剪视频片段
- 🔗 **视频合并** - 多个片段合并成一个
- 🎵 **添加字幕** - 自动或手动添加字幕
- 🎵 **背景音乐** - 添加背景音乐，支持音量调节
- 📐 **尺寸调整** - 适配各种平台（抖音、小红书、B站）
- ⚡ **快速导出** - 多种格式和分辨率

## 使用方法

```json
{
  "tool": "video_clipper",
  "inputs": ["<输入视频路径列表>"],
  "output": "<输出文件路径>",
  "clips": [<裁剪时间点>],
  "subtitle": "<字幕文本>",
  "music": "<背景音乐路径>",
  "aspect_ratio": "9:16",    // 可选，默认 16:9
  "quality": "high"          // 可选，默认 high
}
```

## 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| inputs | array | ✅ | 输入视频文件路径列表 |
| output | string | ✅ | 输出视频文件路径 |
| clips | array | ❌ | 裁剪时间点 [[start, end], ...] |
| subtitle | string | ❌ | 字幕文本 |
| music | string | ❌ | 背景音乐路径 |
| aspect_ratio | string | ❌ | 宽高比，默认 "16:9" |
| quality | string | ❌ | 质量，high/medium/low |
| volume_music | number | ❌ | 音乐音量，0.1-1.0 |

## 宽高比选项

| 比例 | 平台 | 用途 |
|------|------|------|
| 9:16 | TikTok, 抖音, 小红书 | 竖屏短视频 |
| 16:9 | YouTube, B站 | 横屏视频 |
| 1:1 | Instagram | 正方形 |
| 4:5 | Instagram | 竖屏全屏 |

## 示例

```json
{
  "inputs": ["clip1.mp4", "clip2.mp4"],
  "output": "final-video.mp4",
  "clips": [[0, 30], [45, 90]],
  "subtitle": "欢迎观看本期内容",
  "music": "bgm.mp3",
  "aspect_ratio": "9:16",
  "quality": "high"
}
```

## MCP 协议

```json
{
  "name": "video_clipper",
  "description": "自动剪辑视频并生成短视频",
  "parameters": {
    "type": "object",
    "properties": {
      "inputs": {
        "type": "array",
        "description": "输入视频文件路径列表"
      },
      "output": {
        "type": "string",
        "description": "输出视频文件路径"
      },
      "clips": {
        "type": "array",
        "description": "裁剪时间点 [[start, end], ...]"
      },
      "subtitle": {
        "type": "string",
        "description": "字幕文本"
      },
      "music": {
        "type": "string",
        "description": "背景音乐路径"
      },
      "aspect_ratio": {
        "type": "string",
        "description": "宽高比"
      },
      "quality": {
        "type": "string",
        "description": "输出质量"
      }
    },
    "required": ["inputs", "output"]
  }
}
```

## 依赖安装

```bash
pip install moviepy
# 需要先安装 ffmpeg 和 ImageMagick
```
